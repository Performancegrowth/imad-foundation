# ==================================================================
# Imad — Backend dependencies
#
# Core is kept lean. Heavy engineering/ML deps (ezdxf, OpenCV, geopandas,
# openseesp, laspy) are imported lazily inside services so the API and
# tests can boot without them installed.
# ==================================================================

# ── Web framework ──────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic[email]>=2.6
pydantic-settings>=2.2
email-validator>=2.0

# ── ORM / database ─────────────────────────────────────────────────
SQLAlchemy>=2.0
aiosqlite>=0.20

# ── Auth ───────────────────────────────────────────────────────────
PyJWT>=2.8
python-jose[cryptography]>=3.3
passlib>=1.7

# ── HTTP / API clients ─────────────────────────────────────────────
requests>=2.31

# ── CAD / imaging (Sprint 2) ───────────────────────────────────────
ezdxf>=1.3
opencv-python>=4.10
numpy>=1.26
pandas>=2.0
Pillow>=10.0
img2pdf>=0.5
shapely>=2.0          # 2D geometry processing (services/geometry_utils.py)

# ── BIM / IFC import (Sprint 5) ───────────────────────────────────
# Optional: ifcopenshell ships heavy native wheels via its own index
#   `pip install ifcopenshell`. The CAD pipeline degrades gracefully
# (and tests inject a fake module), so the core install stays lean.
# ifcopenshell>=0.7

# ── Survey (Sprint 4) ──────────────────────────────────────────────
pdfplumber>=0.11
geopandas>=0.14
laspy>=2.5
rasterio>=1.3

# ── Structural (Sprint 5) ──────────────────────────────────────────
openseespy>=3.5
scipy>=1.11

# ── Generative design / BOQ / LCA exports (Sprints 6–8) ───────────
deap>=1.4            # NSGA-II multi-objective optimisation
reportlab>=4.0       # branded PDF reports (BOQ, LCA, submissions)
openpyxl>=3.1        # Excel workbooks (Summary / BOQ / BBS sheets)
xlsxwriter>=3.1      # alternative Excel writer

# ── Queue / background jobs (Sprint 14) ────────────────────────────
redis>=5.0           # broker for background jobs (worker service)
celery>=5.3          # optional distributed worker pool behind the API

# ── Migrations (Sprint 14) ─────────────────────────────────────────
alembic>=1.13        # SQLAlchemy schema migrations (upgrade head on boot)

# ── Testing ────────────────────────────────────────────────────────
pytest>=8.0
httpx>=0.27
pint>=0.24         # unit-safe engineering conversions (core/units.py)